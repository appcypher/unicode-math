/**
 * token = { };
 */
class Lexer {
  constructor(code) {
    this.cursor = -1;
  }

  eatChar() {
  }

  lex(code) {
    while (true) {
      switch(char) {
        case '': ;
        default: ;
      }
    }
  }
}
